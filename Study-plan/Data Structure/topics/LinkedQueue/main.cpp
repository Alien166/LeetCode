
int main()
{
    linkedQueue p;
    p.enqueue(20);
    p.enqueue(30);
    p.enqueue(40);
    p.dequeue();
    p.getFront();
    p.enqueue(50);
    p.enqueue(60);
    p.dequeue();
    p.getRear();
    p.getSize();
  
    return 0;
}
